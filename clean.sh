#!/data/data/com.termux/files/usr/bin/bash

# === ЦВЕТА ===
ЗЕЛЁНЫЙ="\033[1;32m"
КРАСНЫЙ="\033[1;31m"
СИНИЙ="\033[1;34m"
ЖЁЛТЫЙ="\033[1;33m"
СБРОС="\033[0m"

# === ФУНКЦИЯ РАМКИ ===
рамка() {
  echo -e "${СИНИЙ}╔════════════════════════════════════════════╗${СБРОС}"
  echo -e "${СИНИЙ}║ ${ЖЁЛТЫЙ}$1${СБРОС}"
  echo -e "${СИНИЙ}╚════════════════════════════════════════════╝${СБРОС}"
}

# === ФУНКЦИЯ ОШИБКИ ===
ошибка() {
  echo -e "${КРАСНЫЙ}╔════════════════════════════════════════════╗"
  echo -e "║ ❌ ОШИБКА: $1"
  echo -e "╚════════════════════════════════════════════╝${СБРОС}"
}

# === НАЧАЛО ===
clear
рамка "🧹 Запуск полной очистки Termux"

# === 1. ОЧИСТКА КЭША ПАКЕТОВ APT ===
рамка "🧼 Очистка кэша пакетов APT..."
if ! apt clean; then
  ошибка "Не удалось очистить apt-кэш"
fi

# === 2. АВТОУДАЛЕНИЕ НЕИСПОЛЬЗУЕМЫХ ПАКЕТОВ ===
рамка "🗑️ Удаление неиспользуемых пакетов..."
if ! apt autoremove -y; then
  ошибка "Не удалось выполнить autoremove"
fi

# === 3. УДАЛЕНИЕ ~/.cache ===
рамка "📂 Очистка папки ~/.cache..."
rm -rf ~/.cache/*

# === 4. УДАЛЕНИЕ ВНУТРЕННЕГО КЭША TERMUX ===
рамка "📂 Очистка внутреннего кэша Termux..."
rm -rf /data/data/com.termux/cache/*

# === 5. УДАЛЕНИЕ МУСОРА НА SD-КАРТЕ (.Trash) ===
рамка "🗑️ Удаление /sdcard/.Trash..."
rm -rf /sdcard/.Trash/* 2>/dev/null

# === 6. ОЧИСТКА ИСТОРИИ BASH/ZSH ===
рамка "📜 Очистка истории команд..."
rm -f ~/.bash_history ~/.zsh_history

# === 7. УДАЛЕНИЕ LOCK- И LOG-ФАЙЛОВ ===
рамка "🔐 Удаление файлов блокировок и логов..."
rm -f /data/data/com.termux/files/usr/var/lib/apt/lists/lock
rm -f /data/data/com.termux/files/usr/var/cache/apt/archives/lock
rm -f /data/data/com.termux/files/usr/var/lib/dpkg/lock

# === 8. ПРОВЕРКА СВОБОДНОГО МЕСТА ===
рамка "📊 Использование памяти:"
df -h | grep -E '^(/|/data|/dev|/sdcard)'

# === ГОТОВО ===
рамка "✅ Очистка Termux завершена!"
